require("utils/animation")

hl.animation({
    leaf = "workspaces",
    enabled = true,
    speed = 1.5,
    bezier = "almostLinear",
    style="slidevert",
})

hl.animation({
    leaf = "windows",
    enabled = true,
    speed = 1.5,
    bezier = "almostLinear",
    style="slidevert",
})
