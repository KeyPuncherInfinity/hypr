hl.config({
    general = {
        layout = "scrolling"
    }
})

hl.config({
    scrolling = {
        fullscreen_on_one_column = true,
        column_width = 0.95,
        focus_fit_method = 1,
        follow_focus = true,
        -- follow_min_visible = 0.03,
        wrap_focus = false,
        wrap_swapcol = false,
        direction = right,
    }
})

hl.config({
    dwindle = {
        preserve_split = true,
    }
})
